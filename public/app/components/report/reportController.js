var reportController = angular.module('reportController', []);

reportController.controller('ReportController', ['$scope','$http', function($scope, $http)
{
  // Document Ready
  angular.element(document).ready(function () {
    
  });
}]);
